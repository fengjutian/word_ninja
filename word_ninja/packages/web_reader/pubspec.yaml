name: web_reader
description: Word Ninja web reader module — split-panel: WebView browser + AI chat
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  phosphor_flutter:
    git:
      url: https://github.com/subarnaPdl/phosphor_icons.git
      ref: main
  flutter:
    sdk: flutter
  core:
    path: ../core
  ui_kit:
    path: ../ui_kit
  ai:
    path: ../ai
  ai_tutor:
    path: ../ai_tutor
  vocabulary:
    path: ../vocabulary
  flutter_riverpod: ^2.6.1
  flutter_markdown: ^0.7.7+1
  webview_flutter: ^4.10.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
